{
  "name": "synaptic-memory-lab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Can a connection remember? An interactive instrument for seeing synaptic short-term memory form, store and decay \u2014 built on a tiny, honest, reusable plasticity simulation.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit",
    "e2e": "node scripts/e2e-verify.mjs",
    "verify": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "playwright": "^1.63.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
